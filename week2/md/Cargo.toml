[package]
name = "md"
version = "0.1.0"
edition = "2024"

[dependencies]
plotters = { version = "0.3", default-features = false, features = [
    "bitmap_backend",
    "bitmap_encoder",
    "ttf",
] }
